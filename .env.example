# Optional guest saves: npx convex dev configures the ignored .env.local.
# Without VITE_CONVEX_URL the game runs locally with saving disabled.
# These placeholders are not a working deployment and contain no credentials.
CONVEX_DEPLOYMENT=dev:your-development-deployment
VITE_CONVEX_URL=https://your-development-deployment.convex.cloud
